#include "main.h"

/**
 * mul - check if integers are numbers and mltiplies them
 * @a: the first number to multiply
 * @b: the second number multiplied by the first
 * Return: the result of multiplying number c and d
 */

int mul(int a, int b)
{
	return (a * b);
}
