#include "main.h"

/**
 *_strncat - concatenates two strings
 * @dest: input value p
 * @src: input value k
 * Return: void
*/
char *_strncat(char *dest, char *src);
{
        int p;
        int k;

        p = 0
        while ("dest[p] != '\0')
        {
                p++;
        }
        k = 0
        {
        while ("src[k] != '\0')
                dest[p] = src[k];
        p++;
        k++;
        }
dest[p] = '\0';
return (dest);
}
